@model IEnumerable<FamousFolks.Models.FolksExpertise>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExpId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExpId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFolkExp }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFolkExp }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFolkExp })
        </td>
    </tr>
}

</table>
